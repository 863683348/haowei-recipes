# dependencies
/node_modules
/node_modules-old
/.pnp
.pnp.*

# 单语版旧页面归档（迁移到 [locale] 后保留备份，不提交）
/_archive-single-lang/

# 图片原始 PNG/SVG 归档（public 只保留压缩后 webp，不提交）
/_archive-images/

# 构建缓存备份（safe-delete 钩子无法删除时的临时归档，不提交）
/.next-old-*/
/.next*/

# testing
/coverage

# next.js
/.next/
/.next-broken/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 每日菜谱生成临时批 JSON
/scripts/.recipe-batch-*.json

# 本地 dev server 日志
/dev.log

# 生图/PDF 导出预览（临时，不提交）
/.pdf_preview/
/.recipe-export/
/.v_agnes_test/
/.v_*.html
/verify_out/
/verify*.js
